{
  "name": "whatsapp-gateway-for-n8n",
  "version": "1.0.0",
  "description": "WhatsApp Gateway service for n8n automation workflows",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "lint": "eslint .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker:build": "docker build -t whatsapp-gateway:latest .",
    "docker:run": "docker run -p 3000:3000 --env-file .env whatsapp-gateway:latest",
    "docker:compose": "docker-compose up -d",
    "docker:logs": "docker-compose logs -f",
    "docker:stop": "docker-compose down"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^8.0.0",
    "qrcode": "^1.5.3",
    "winston": "^3.11.0",
    "whatsapp-web.js": "^1.23.0",
    "wwebjs-mongo": "^1.1.0"
  },
  "devDependencies": {
    "eslint": "^8.52.0",
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
